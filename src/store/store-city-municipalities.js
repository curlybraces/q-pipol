const state = {
  city_municipalities: [] //  localStorage.getItem("city_municipalities") || [],
};

const mutations = {};

const actions = {};

const getters = {
  //
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
