const state = {
  gad_readinesses: [] // localStorage.getItem("gad_readinesses") || [],
};

const mutations = {

};

const actions = {
  
};

const getters = {
  //
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
