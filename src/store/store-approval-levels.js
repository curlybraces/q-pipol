import { approvalLevels } from "src/statics/json/approvalLevels.json";

const state = {
  approvalLevels: approvalLevels
};

const mutations = {};

const actions = {};

const getters = {
  //
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
